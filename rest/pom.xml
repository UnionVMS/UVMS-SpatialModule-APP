<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>rest</artifactId>
    <name>${project.parent.artifactId}-rest</name>
    <packaging>war</packaging>

    <parent>
        <groupId>eu.europa.ec.fisheries.uvms.spatial</groupId>
        <artifactId>spatial</artifactId>
        <version>0.5.5-SNAPSHOT</version>
    </parent>

    <dependencies>
    
        <dependency>
            <groupId>eu.europa.ec.fisheries.uvms</groupId>
            <artifactId>usm4uvms</artifactId>
            <scope>provided</scope>
        </dependency>
        
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt</artifactId>
        </dependency>
        
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>spatial-model</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>service</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>eu.europa.ec.fisheries.uvms</groupId>
            <artifactId>uvms-test</artifactId>
        </dependency>

        <dependency>
            <groupId>javax</groupId>
            <artifactId>javaee-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
        </dependency>

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct-processor</artifactId>
        </dependency>

        <dependency>
            <groupId>eu.europa.ec.fisheries.uvms.movement</groupId>
            <artifactId>movement-model</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>eu.europa.ec.fisheries.uvms.asset</groupId>
            <artifactId>asset-model</artifactId>
            <scope>provided</scope>
        </dependency>

		<dependency>
			<groupId>org.hibernate</groupId>
			<artifactId>hibernate-validator</artifactId>
		</dependency>

         <dependency>
			 <groupId>org.jboss.resteasy</groupId>
    		<artifactId>resteasy-jaxb-provider</artifactId>
		</dependency>

        <dependency>
            <groupId>org.jboss.resteasy</groupId>
            <artifactId>resteasy-multipart-provider</artifactId>
        </dependency>

		<dependency>
		    <groupId>org.jboss.resteasy</groupId>
		    <artifactId>jaxrs-api</artifactId>
		</dependency>

    </dependencies>

    <build>
        <finalName>${project.name}-${project.version}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.1</version>
                <configuration>
                    <source>1.7</source>
                    <target>1.7</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>2.4</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-maven</artifactId>
                <version>1.1.10</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <!-- USE following command to run this profile: mvn clean install -P generate-rest-doc -Dhostname=127.0.0.1 -Dport=8080  -->
    <profiles>
        <profile>
            <id>generate-rest-doc</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>2.9.1</version>
                        <executions>
                            <execution>
                                <id>generate-service-docs</id>
                                <phase>generate-resources</phase>
                                <configuration>
                                    <doclet>com.carma.swagger.doclet.ServiceDoclet</doclet>
                                    <docletArtifact>
                                        <groupId>com.carma</groupId>
                                        <artifactId>swagger-doclet</artifactId>
                                        <version>1.0.4.1</version>
                                    </docletArtifact>
                                    <reportOutputDirectory>src/main/webapp</reportOutputDirectory>
                                    <useStandardDocletOptions>false</useStandardDocletOptions>
                                    <additionalparam>-apiVersion ${project.version}
                                        -docBasePath /${project.parent.artifactId}/apidocs
                                        -apiBasePath /${project.parent.artifactId}/rest</additionalparam>
                                </configuration>
                                <goals>
                                    <goal>javadoc</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
		<profile>
            <id>weblogic</id>
            <build>
                <plugins>		
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-war-plugin</artifactId>
						<version>2.4</version>
						<configuration>
							<generateApplicationXml>false</generateApplicationXml>						
							<webXml>${basedir}/../container-conf/temp/web.xml</webXml>							
							<packagingExcludes>WEB-INF/lib/*.jar</packagingExcludes>				
						</configuration>
					</plugin>
					
					
					<plugin>
					<artifactId>maven-antrun-plugin</artifactId>
					<version>1.7</version>
					<executions>
					  <execution>
						<id>configPackage</id>
						<phase>validate </phase>
						<configuration>
						  <tasks>
						  <!-- check if config is available, if not fail -->
						  <available file="${configDirectory}/weblogic-application.xml" property="configIsAvailable" />
						  <fail message="Unable to locate configuration: ${configDirectory}/weblogic-application.xml" unless="configIsAvailable" />

						  <available file="${configDirectory}/application.xml" property="configIsAvailable" />
						  <fail message="Unable to locate configuration: ${configDirectory}/application.xml" unless="configIsAvailable" />
						  

						  <!-- copy the files from the config dir to the webapp dir -->
						  <echo message="Copying data dir from ${configDirectory}" />
						  <copy todir="${earSourceDirectory}" >
							 <fileset dir="${configDirectory}" includes="**/**" />
						  </copy>
						  
						  
						  <replace file="${earSourceDirectory}/application.xml" token="@projectVersion@" value="${project.parent.version}"/> 
						  <replace file="${earSourceDirectory}/application.xml" token="@usm4uvmsVersion@" value="${usm4uvms.version}"/> 
						  
						  
						  <mkdir dir="${basedir}/../container-conf/temp" />
						  
						  <copy todir="${basedir}/../container-conf/temp" >
							 <fileset dir="${basedir}/../rest/src/main/webapp/WEB-INF" includes="web.xml" />
						  </copy>
						  
						  <available file="${basedir}/../container-conf/temp/web.xml" property="configIsAvailable" />
						  <fail message="Unable to locate configuration: ${basedir}/../container-conf/temp/web.xml" unless="configIsAvailable" />
						  
						  <replace file="${basedir}/../container-conf/temp/web.xml" token="3_1" value="3_0"/> 
						  <replace file="${basedir}/../container-conf/temp/web.xml" token="3.1" value="3.0"/> 
						  <replace file="${basedir}/../container-conf/temp/web.xml" token="http://xmlns.jcp.org/xml/ns/javaee" value="http://java.sun.com/xml/ns/javaee"/> 
						  
			 
						  </tasks>
						</configuration>
						<goals>
						  <goal>run</goal>
						</goals>
					  </execution>
					</executions>
				  </plugin>					
					
                </plugins>							
            </build>				
		</profile>
		 
		
    </profiles>

</project>
